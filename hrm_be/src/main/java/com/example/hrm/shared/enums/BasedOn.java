package com.example.hrm.shared.enums;

public enum BasedOn {
    MINUTE,   // đi muộn, về sớm
    DAY,      // nghỉ không phép
    TIME,     // số lần
    FIXED,     // phạt cố định
    LATE_MINUTES,
    EARLY_LEAVE_MINUTES,
    VOID_BASE_SALARY,
    VOID_OVERTIME
}
